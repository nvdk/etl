/**
 * Plugins templates are loaded from JAR files, as such they can not be changed,
 * they are read only. Each plugin has a definition, configuration,
 * configuration description and dialog files.
 */
package com.linkedpipes.etl.storage.template.plugin;
